import 'package:flutter/material.dart';

export 'package:bla_bla_in_english/sentence_format.dart' show sentenceDivider;

const Color primaryColor = Color(0xFFA5D8FF);
const Color tertiaryColor = Color(0xFFFFEC99);
const Color correctAnswerColor = Color(0xFFB2F2BB);
const Color wrongAnswerColor = Color(0xFFFFC9C9);

/// Colour for a "near miss" answer — right neighbourhood, wrong nuance.
const Color nearAnswerColor = tertiaryColor;

/// Neutral fill for an option that has not been picked.
const Color optionColor = Color(0xFFF1F3F5);
const Color optionBorderColor = Color(0xFFDEE2E6);
